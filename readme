
# 1. batch_files_rename_specify_text.py

> 批量替换文件名中的字符串

参数意义：
1. path为批量替换文件的目录
2. src_str为文件名中的源字符串
3. dst_str为要替换成的目的字符串

# 2. batch_files_rename_pre_space.py

> 批量删除文件名中空格之前的字符串

参数意义：
1. path为批量替换文件的目录
